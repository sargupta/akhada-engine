# V1 placeholder. Local-only V0 needs no GCP resources.
# Module entrypoint will land at V1; see infra/README.md for the planned modules.

terraform {
  required_version = ">= 1.9"
  # required_providers, backend, etc. land V1.
}
